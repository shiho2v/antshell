# 개발 원칙 및 규칙 (Core Development Principles)

본 문서는 코드 작성·설계·협업 방식에 관한 핵심 원칙과 규칙을 정리합니다. 특정 도구·기술 스택·운영 환경에 무관하게 적용되는 보편적 기준입니다. 세부 구현 지침은 `WEB_DEV_GUIDELINES.md`를 참조하십시오.

---

## 1. 코드 스타일 `[필수]`

**변수명**
- 한 글자 변수 사용 금지 (`i`, `j`, `e` 등)
- 도메인 내부 약어 금지, 의미가 드러나는 풀네임 사용
- 프레임워크 공식 관례로 굳어진 약어는 예외 허용
- 루프 변수도 `index`, `iter`, `count` 등 명시적으로 작성

**공백·블록**
- 연속 빈 줄 최대 1줄
- 중괄호 언어: 단일 실행문이라도 중괄호 생략 금지
- Python: `if`/`for` 본문 인라인 작성 금지, 다음 줄에 들여쓰기

---

## 2. 설계 원칙 `[필수]`

**YAGNI — 과도한 추상화 금지**
- "미래에 필요할 것 같다"는 예측만으로 추상 레이어를 미리 만들지 않는다
- 구현체가 하나뿐일 때 인터페이스를 선제적으로 만들지 않는다
- 단, 테스트 Mocking 또는 복수 구현 등록이 실제로 필요한 시점에는 도입 허용
- 최소 구현에 집중하고, 변경이 실제로 발생할 때 리팩토링한다

**수술적 변경 — 작업 범위 엄수**
- 기능 추가·버그 수정 시 작업 범위 밖 코드를 건드리지 않는다
- 하나의 커밋·PR은 하나의 논리적 변경만 포함한다

**점진적 설계 — BDUF 금지**
- 시스템 전체를 한 번에 설계하지 않는다
- 기능 단위로 분리하여 End-to-End를 완결한 뒤 다음 기능으로 확장한다

---

## 3. 의존성 주입 `[필수]`

- 전역 인스턴스를 코드 내부에서 직접 찾아 참조하지 않는다 (Service Locator 금지)
- 의존성은 생성자 매개변수를 통해 주입받는 것을 기본으로 한다
- 프레임워크가 공식적으로 정의한 DI 패턴은 예외로 허용한다

---

## 4. 무상태 아키텍처 `[참고]`

- API 서버 메모리에 클라이언트 상태를 저장하지 않는다
- 요청 처리에 필요한 컨텍스트는 요청 자체(헤더·토큰)에 포함한다
- 휘발성 데이터(캐시, 락 등)는 인메모리 저장소에 저장한다
- RDBMS는 정합성·트랜잭션이 필요한 핵심 비즈니스 데이터 전담으로 한정한다

---

## 5. 비동기 처리 `[참고]`

- API 응답 시간 내에 무거운 쓰기 작업을 동기 처리하지 않는다
- 대량 데이터 적재는 메시지 큐 → 백그라운드 워커를 통해 지연 처리한다

---

## 6. 품질 보증 `[필수]`

**입력 검증**
- 모든 외부 입력을 신뢰하지 않는다
- 백엔드 진입 시 스키마·타입을 엄격하게 검증한다
- 스펙이 모호할 때 스스로 가정하지 않는다. 협의 후 명문화하고 구현한다

**테스트**
- 버그 수정 전 해당 버그를 재현하는 실패 테스트를 먼저 작성한다
- 신규 기능은 Red → Green → Refactor 사이클을 권장한다
- 핵심 API·서비스 레이어·유효성 검증은 테스트 필수, UI·유틸리티는 권장

**CI/CD**
- 커밋 전 로컬에서 린트·테스트를 선행한다
- 병합 전 CI에서 린트·타입 검사·테스트가 모두 통과되어야 한다

---

## 7. 에러 핸들링 `[필수]`

- 처리되지 않은 예외가 기본 에러 응답을 노출하지 않도록 전역 예외 핸들러를 구축한다
- 모든 에러 응답은 단일 JSON 구조로 반환한다 (RFC 7807 준수)
- 스택 트레이스·DB 에러 메시지는 클라이언트 응답에 포함하지 않는다

---

## 8. API 문서화 `[필수]`

- API 명세는 소스 코드에서 자동 생성한다. 노션·위키에 수동으로 타이핑하지 않는다
- 단, 회의록·발표 자료·스터디 일지 등 협업 문서는 Notion 사용을 허용한다
- OpenAPI 규격을 채택하여 코드 변경 시 명세가 자동으로 갱신되도록 한다

---

## 9. 보안 `[필수]`

**인증·인가**
- 보호 엔드포인트 진입 시 토큰 서명·만료를 반드시 검증한다
- 인증과 인가를 분리하여 설계한다
- 기본은 RBAC, 역할만으로 표현하기 어려운 복잡한 권한이 필요할 때만 ABAC를 도입한다

**웹 취약점**
- 쿼리는 파라미터 바인딩을 사용한다. 문자열 직접 보간으로 쿼리를 구성하지 않는다
- 사용자 입력은 이스케이프 처리 후 사용한다
- CORS Origin은 와일드카드 대신 화이트리스트로 명시 관리한다

**시크릿·의존성**
- 시크릿을 소스 코드에 하드코딩하지 않는다. 환경 변수로 주입한다
- `.env` 파일은 버전 관리에서 제외한다
- 패키지 Lock 파일을 Git에 커밋한다
- CI에 의존성 취약점 스캔을 포함한다

---

## 10. 로깅 `[참고]`

- 로그는 JSON 구조체로 출력한다
- 모든 로그에 `timestamp`, `level`, `traceId`를 포함한다
- ERROR: 즉각 대응이 필요한 장애 / WARN: 비정상이지만 장애 아닌 상황 / INFO: 주요 비즈니스 이벤트 / DEBUG: 프로덕션 비활성화
- 비밀번호·개인식별정보(PII)는 로그에 기록하지 않는다

---

## 11. Git 워크플로우 `[필수]`

**브랜치**
- GitHub Flow 채택: `main` + 기능/버그 단위 단기 브랜치
- `main`에 직접 커밋하지 않는다. 모든 변경은 PR을 통해 병합한다
- 브랜치명: `feature/<주차>-<이름>-<기능명>`, `fix/<주차>-<이름>-<버그내용>`, `hotfix/<이름>-<내용>`
- 예시: `feature/01-alice-dart-api`, `fix/03-bob-chart-null`, `hotfix/carol-env-missing`

**커밋**
- Conventional Commits 형식 준수: `<type>(<scope>): <subject>`
- 타입: `feat`, `fix`, `refactor`, `test`, `chore`, `docs`, `style`
- 하나의 커밋에 하나의 논리적 변경만 담는다

---

## 12. 단계별 설계 `[필수]`

기능 구현 착수 전 아래 순서로 설계를 완결한다.
> **스터디 기간 한정**: 매주 발표 준비 시 5단계 전체를 별도 문서로 작성하기 어려운 경우, 각 단계의 핵심 항목만 확인하는 체크리스트 형식으로 운영해도 무방하다. 산출물 분량은 기능 복잡도에 비례하여 조정한다.

1. **요구사항 명세**: 기능 목표, 비기능 요건, Out of Scope 정의, 모호한 사항 사전 해소
2. **데이터 모델**: 엔티티·관계 정의, 기존 스키마 영향 분석, 인덱스 전략
3. **API 계약**: 엔드포인트·요청/응답 스키마 확정, 에러 케이스 목록화, 연동팀 검토
4. **로직 흐름**: 서비스 처리 순서, 트랜잭션 경계, 부수 효과(Side Effect) 식별
5. **설계 검토**: 팀 리뷰 후 구현 착수, 변경 시 코드와 문서를 동시에 갱신

---

## 13. 리팩토링 `[참고]`

**시점**
- 기능이 정상 동작하고 테스트가 통과된 상태에서만 시작한다
- 수술적 변경 원칙이 우선한다. 범위 밖 개선은 별도 커밋·PR로 분리한다
- Rule of Three(중복 3회 이상), 단일 책임 위반, 이해 난이도 증가 시 검토한다

**절차**
- 리팩토링 대상에 테스트가 없으면 먼저 작성한다
- 작은 단위로 나눠 각 단계마다 테스트 통과를 확인한다
- `refactor:` 커밋에 동작 변경을 포함하지 않는다

**금지**
- 배포 태그 생성 후 안정화 기간 중
- 복잡한 로직에 테스트가 없는 상태
- 코드의 동작 의도를 파악하지 못한 상태
